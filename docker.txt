docker container --help
docker --version
docker info
docker image ls
docker container ls
docker container ls --all
docker container ls -aq
docker system info 
docker history alpine
docker image inspect alpine
docker image build -t myname (directory or git repo)
docker container run -d --name myname -p (host port)8080:(container port)8080  (-d is for to run in background/demon)
docker container run -it alpine sh (it is for interactive terminal , will open a shell to the container)
docker container start name/id
docker container stop name/id

if we type exit inside the container terminal the container also shuts down
ctrl+P+Q to come out of the container terminal without terminating the container

dotcloud
lxc
libcontainer

-------------------------------
put these lines in your .bashrc file

kpython(){
  docker run -v $PWD:/tmp/working -w=/tmp/working --rm -it kaggle/python python "$@"
}

ikpython() {
  docker run -v $PWD:/tmp/working -w=/tmp/working --rm -it kaggle/python ipython
}

kjupyter() {
  (sleep 3 && open "http://$(docker-machine ip docker2):8888")&
  docker run -v $PWD:/tmp/working -w=/tmp/working -p 8888:8888 --rm -it kaggle/python jupyter notebook --no-browser --ip="\*" --notebook-dir=/tmp/working
}